# 问题 & TOPICS

一些 Crater 相关的问题或值得探讨的话题。

## 一、调度

### 1. 网络拓扑感知的调度

是否支持网络拓扑结构感知的调度，比如我要求他们连接在同一台交换机上，或者要求他们可以通过两跳之内链接？

Volcano 允许网络拓扑感知的调度，但是 Crater 应该没有使用这个特性。

#### 1.1 网络拓扑结构的自动发现

这是否能够实现集群网络拓扑的自动感知？

Volcano 似乎可以支持网络拓扑的自动感知，示例中使用一个树表达了网络结构，但是数据结构上似乎没有限定这一定要是树，应该可以是图。

### 2. 虚拟 GPU

如果有大量的作业对于 GPU 计算、存储的利用率都没有超过 50%，那么我是否可以直接把一些节点上的单个 GPU 虚拟成两个进行上报和调度。

### 3. 回填与抢占

比如一个 8 卡作业在排队，我们是否可以在保证这个作业的开始时间的前提下，使用“可被抢占式作业”回填空余的卡，然后当这个大作业该开始时，停止这些作业。

现在似乎已经有基本的抢占和回填功能，但还没启用。

或许可以约定一套 checkpoint 和信号机制，当作业即将被抢占而停止时，用某种方式通知作业，提醒作业在规定时间内保存数据，告诉它它即将被停止。

> 目前企业中似乎支持保存用户容器的全部状态，包括存储情况、GPU 状态等等，然后再停止或者抢占，用于 GPU 超售。